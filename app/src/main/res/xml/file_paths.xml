<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Only the one folder the stats share card is written to. Nothing else the app stores is
         meant to leave it, so nothing else is exposed here. -->
    <cache-path
        name="shared_stats"
        path="shared_stats/" />
</paths>
