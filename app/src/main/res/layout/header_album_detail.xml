<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:paddingHorizontal="32dp"
    android:paddingTop="8dp"
    android:paddingBottom="24dp">

    <com.google.android.material.card.MaterialCardView
        style="?materialCardViewFilledStyle"
        android:layout_width="220dp"
        android:layout_height="220dp"
        app:cardCornerRadius="4dp"
        app:cardElevation="0dp"
        app:cardBackgroundColor="@android:color/transparent"
        app:strokeColor="#0DFFFFFF"
        app:strokeWidth="0.7dp">

        <ImageView
            android:id="@+id/albumCover"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="?attr/contrast_colorBackgroundContainer"
            android:contentDescription="@string/dialog_album"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_default_cover" />

    </com.google.android.material.card.MaterialCardView>

    <TextView
        android:id="@+id/albumTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:fontFamily="sans-serif"
        android:gravity="center"
        android:maxLines="2"
        android:ellipsize="end"
        android:textColor="?attr/contrast_colorOnSurface"
        android:textFontWeight="400"
        android:textSize="22sp"
        tools:text="Veeram (Original Motion Picture Soundtrack)" />

    <TextView
        android:id="@+id/albumArtist"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:fontFamily="sans-serif"
        android:gravity="center"
        android:maxLines="1"
        android:ellipsize="end"
        android:textColor="@android:color/white"
        android:textFontWeight="400"
        android:textSize="16sp"
        tools:text="Devi Sri Prasad" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:layout_gravity="center_horizontal"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/albumMeta"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:maxLines="1"
            android:ellipsize="end"
            android:textColor="@color/song_option_album_text"
            android:textSize="14sp"
            tools:text="7 Songs • 2013 • " />

        <ImageView
            android:id="@+id/hiResIcon"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_marginEnd="4dp"
            android:adjustViewBounds="true"
            android:contentDescription="@null"
            android:src="@drawable/apple_lossless_seeklogo"
            android:visibility="gone"
            app:tint="@color/song_option_album_text" />

        <TextView
            android:id="@+id/albumFormat"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:maxLines="1"
            android:ellipsize="end"
            android:textColor="@color/song_option_album_text"
            android:textSize="14sp"
            tools:text="Lossless" />

    </LinearLayout>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/shuffleButton"
            android:layout_width="52dp"
            android:layout_height="52dp"
            android:layout_marginEnd="16dp"
            android:insetLeft="0dp"
            android:insetTop="0dp"
            android:insetRight="0dp"
            android:insetBottom="0dp"
            app:backgroundTint="?attr/contrast_colorBackgroundContainer"
            app:cornerRadius="26dp"
            app:icon="@drawable/ic_shuffle"
            app:iconGravity="textStart"
            app:iconPadding="0dp"
            app:iconSize="24dp"
            app:iconTint="?attr/contrast_colorOnSurface" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/playButton"
            android:layout_width="wrap_content"
            android:layout_height="52dp"
            android:layout_marginStart="16dp"
            android:minWidth="150dp"
            android:paddingHorizontal="24dp"
            android:text="@string/play"
            android:textColor="@android:color/black"
            android:textSize="16sp"
            android:textStyle="bold"
            app:backgroundTint="@android:color/white"
            app:cornerRadius="26dp"
            app:icon="@drawable/ic_play_arrow_filled"
            app:iconGravity="textStart"
            app:iconTint="@android:color/black" />

    </LinearLayout>

</LinearLayout>
