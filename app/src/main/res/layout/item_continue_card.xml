<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="@dimen/book_cover_size_large"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    android:orientation="vertical"
    android:paddingHorizontal="6dp">

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/card_cover"
        android:layout_width="@dimen/book_cover_size_large"
        android:layout_height="@dimen/book_cover_size_large"
        android:contentDescription="@null"
        android:scaleType="centerCrop"
        app:shapeAppearanceOverlay="@style/ShapeAppearance.Material3.Corner.ExtraSmall"
        app:strokeColor="?attr/contrast_colorOutline"
        app:strokeWidth="0.5dp"
        tools:src="@drawable/ic_default_cover" />

    <ProgressBar
        android:id="@+id/card_progress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:max="100"
        android:visibility="gone"
        tools:progress="40"
        tools:visibility="visible" />

    <TextView
        android:id="@+id/card_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:ellipsize="end"
        android:maxLines="2"
        android:textAppearance="?attr/textAppearanceBodyMedium"
        android:textColor="?attr/contrast_colorOnSurface"
        android:textFontWeight="600"
        tools:text="Book title that might wrap to two lines" />

    <TextView
        android:id="@+id/card_author"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:textAppearance="?attr/textAppearanceBodySmall"
        android:textColor="?attr/contrast_colorOnSurfaceSecondary"
        tools:text="Author name" />

</LinearLayout>
